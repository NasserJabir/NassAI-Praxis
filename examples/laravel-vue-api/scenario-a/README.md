# Scenario A — Without Praxis

This is the final artifact of four independent sessions. The code remains plausible but exhibits context loss: route naming drift, inconsistent response envelopes, direct model access in a controller, a missing product validation regression test, and an unguarded order link in the frontend.
